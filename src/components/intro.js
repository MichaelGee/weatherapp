import React from "react";

const Intro = () => {
  return (
    <div>
      <div className="container intro text-center">
        <h4>This gets you the current weather condition in a given country.</h4>
      </div>
    </div>
  );
};

export default Intro;
